import {runtimeConfig} from "@/lib/runtime/config";
export const dynamic='force-dynamic';
export default function Privacy(){
 const config=runtimeConfig();
 return <main className="demo-guide"><h1>Privacy and your trip</h1><p>This installation is operated by {config.operator}. Converge helps a group compare availability; an availability answer is not an attendance RSVP.</p><h2>{config.mode==='demo'?'Local demo':'Hosted installation'}</h2>{config.mode==='demo'?<p>All starter people and calendar events are invented. Your edits stay in a database on this computer. The demo does not connect to Google, send email, or call an AI provider. The persona selector is only available on a loopback address.</p>:<p>Google sign-in identifies your account and requests read-only calendar access. Calendar checks happen when you request them. Tokens remain server-side and are not included in public browser session data. The operator supplies the database, mail service and optional AI configuration.</p>}<h2>Who can see what</h2><p>Anyone holding a join link can see the trip, organizer and respondent display names, answers, favorites and response times. Group views do not include respondent email addresses. The organizer can see contact addresses and private required-person settings. Keep private return links secret: possession authorizes editing that response. Typing an email address never grants ownership.</p><p>Calendar event details are private to the account doing the scan. They do not become shared availability automatically. If enabled, explicitly requesting AI history grouping sends selected event text to Gemini. Ordinary date ranking and group comparisons make no AI calls.</p><h2>Saved data and delivery</h2><p>Trip decisions, responses and notification jobs persist until the operator removes them. Complete current scans may be cached for five minutes, history grouping for seven days, and quotas/budget records for operational protection. Scheduled maintenance must run to clean expired records and recover notifications. This software does not promise automatic erasure of every provider backup.</p><p>Confirming a date queues messages only for respondents with contact addresses. Sending can be disabled. Delivery is at least once, so a retry after a timeout may duplicate a message. Reopening cancels unsent outdated decisions but cannot recall an accepted message or an imported calendar file.</p><h2>Copies, removal and contact</h2><p>There is no self-service account export/deletion screen. The installation guide explains local backups. Contact the operator to request removal of your trip or response. Local browser drafts contain planning settings but no raw calendar events.</p>{config.contact?<p>Contact <a href={'mailto:'+config.contact}>{config.contact}</a> for this installation.</p>:<p>This is your local copy. Close its terminal to stop the server; keep the data folder private. See the included backup guide before removing saved records.</p>}<p><a href="/">Back to Converge</a></p></main>;
}
